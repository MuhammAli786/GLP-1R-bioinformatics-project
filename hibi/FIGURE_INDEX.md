# Figure Index — HIBI

Every figure in `figures/` and the script that generated it. All figures are at
the **LFC0.2** threshold (padj < 0.05), matching the GLP-1R project convention.
PNG + vector PDF provided for every figure. Each maps to the equivalent panel in
`GLP-1R-bioinformatics-project`.

| File (in `figures/`) | GLP-1R equivalent | What it is | Generated by |
|---|---|---|---|
| `Cnet_Consensus_TopFrequentGenes_LFC0.2` | (`06f` output; genes match Fig 11) | Consensus-signature concept network — the 30 most frequently recurring HIBI consensus genes. Node size = number of groups the gene recurs in; colour = mean log2FC. Terms from enriching just those genes, chosen by greedy set-cover. | `code/01_meta_analysis_pipeline/06f_consensus_frequency_cnet.py` |
| `Cnet_BBB_FocusedTerms_LFC0.2` | Fig 8B | Restricted concept network — blood-brain-barrier / MMP gene set (35/44 curated genes present in HIBI consensus) | `code/01_meta_analysis_pipeline/06d_focused_pathway_cnet.py BBB` |
| `Cnet_Inflammatory_FocusedTerms_LFC0.2` | Fig 8A | Restricted concept network — JAK-STAT3 inflammatory gene set (22/33 present) | `…/06d_focused_pathway_cnet.py Inflammatory` |
| `Cnet_Survival_FocusedTerms_LFC0.2` | Fig 8C | Restricted concept network — PI3K-Akt pro-survival gene set (41/68 present) | `…/06d_focused_pathway_cnet.py Survival` |
| `Cnet_IonChannel_FocusedTerms_LFC0.2` | Fig 8D | Restricted concept network — ion-channel / neurogenesis gene set (18/30 present) | `…/06d_focused_pathway_cnet.py IonChannel` |

## GOplot figures (`figures_goplot/`)

31 figures (PNG + PDF each), rendered by real GOplot 1.0.2 in R — the same
function calls and parameters as the GLP-1R project's `goplot_analysis.R`.

| Folder | Figures | GLP-1R equivalent |
|---|---|---|
| `Consensus/` | `GOChord_Consensus`, `_KEGG`, `_Reactome`, `_AllEnrichments`, `GOBubble_consensus_top20percat`, `GOCircle_consensus_top10`, `GOHeat_consensus` | Fig 7 / Fig 11 |
| `BBB/` | `GOChord_BBB`, `_KEGG`, `_Reactome`, `_AllEnrichments`, `GOBubble_BBB_top5percat`, `GOHeat_BBB` | GOplot BBB set |
| `Inflammatory/` | same 6 | GOplot Inflammatory set |
| `Survival/` | same 6 | GOplot Survival set |
| `IonChannel/` | same 6 | GOplot IonChannel set |

Generated by `code/04_goplot_chord/prepare_goplot_data.py` (input tables) then
`code/04_goplot_chord/goplot_analysis.R` (rendering). See that folder's
`README.txt` for the full plot inventory and reproduction steps.

## Method notes

**Consensus Cnet** (`06f`) — genes ranked purely by recurrence (`n_groups`),
*not* by log2FC and *not* by enrichment strength; this is the same gene ranking
GLP-1R uses for its consensus heatmap and GOChord. All 30 top-frequent genes are
forced into the graph as nodes even if they land in no enriched term, so the
figure always shows the full signature. Terms are picked by greedy set-cover over
those genes (max 12).

**Restricted / focused Cnets** (`06d`) — terms come from enriching *only* that
pathway's HIBI consensus genes (the way the GOplot chord does), rather than
keyword-filtering a whole-consensus enrichment. Terms are then taken in
adjusted-p order, dropping any whose pathway-gene overlap has Jaccard ≥ 0.8 with
an already-selected term, so the network stays readable. Max 12 terms, max 20
genes per term edge list.

Both use the unmodified `cnet_style.py` and `cnet_gene_lists.py` copied from the
GLP-1R project, so styling, colours, node sizing, spring layout (k=3.5, 150
iterations, seed=42) and library colour legend are identical between the two
projects. Enrichment via Enrichr (gseapy), mouse organism, across
GO-BP/MF/CC + KEGG_2021_Human + Reactome_2022.
